{
  "name": "@rovrs/archival-worker",
  "version": "1.0.0",
  "description": "Archival worker for cleaning up expired links",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "eslint": "^9.0.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@rovrs/shared": "1.0.0"
  }
}