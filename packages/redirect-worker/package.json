{
  "name": "@rovrs/redirect-worker",
  "version": "1.0.0",
  "description": "Redirect worker for ROVRS link shortener",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.7.0",
    "vitest": "^1.6.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@rovrs/shared": "1.0.0"
  }
}
